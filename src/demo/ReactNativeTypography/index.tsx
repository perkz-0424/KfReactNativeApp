import React from "react";
import {SafeAreaView, ScrollView, Text} from "react-native";
import {material} from 'react-native-typography'


const ReactNativeTypography = () => {
    React.useEffect(() => {

    }, []);
    return <SafeAreaView>
        <ScrollView>

        </ScrollView>
    </SafeAreaView>;
};

export default ReactNativeTypography;