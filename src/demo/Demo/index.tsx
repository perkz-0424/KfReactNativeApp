import React from "react";
import {SafeAreaView, ScrollView} from "react-native";


const Demo = () => {
    React.useEffect(() => {

    }, []);
    return <SafeAreaView>
        <ScrollView>

        </ScrollView>
    </SafeAreaView>;
};

export default Demo;